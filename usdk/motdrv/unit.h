#define r        // revolutions
#define RPM      // revolutions per minute
#define RPS      // revolutions per second
#define PPS      // pulse per second (Hz)
#define AMP      // current: ampere, A

#define torque   // N*m

#define torque   // N*m
#define inertia  // 惯量 kg*m^2

// rad不是物理量单位，是角度单位，以rad做角度单位时，rad无需写明，
// 除非强调时。即角速度单位就是s^-1，角加速度单位就是 s^-2

#define N      // 牛 kg*m/s^2
#define omiga  // 角速度 rad/s, Ω
#define a      // 角加速度 rad/s^2

               // torque = inertia *  a

// M是转矩，I 是转动惯量，B是角加速度。
// M = I*B
